config:
  dashboard:
    altitude: '+0'
    message:
      halt: "プログラムを停止します"
    font:
      size: 10
    color:
      background: "#00AAFF"
      lit: "#FF7700"
      unlit: "#000000"
      mask: "#555555"

    launch_setup:
      label: "パネル"
      package: rtk_tools
      file: panel.launch

  panel:
    color:
      background: "#00AAFF"
    dump: solve.yaml
    dump_dir@: /wpc/recipeName
    dump_ver@: /wpc/recipeNumber
    dump_prefix: WPC/recipe
